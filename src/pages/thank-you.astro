---
import BaseLayout from '../layouts/BaseLayout.astro';
---

<BaseLayout
  title="Thank You | OnlineTranslation.ae"
  description="Thank you for contacting OnlineTranslation.ae. We will respond to your inquiry within 2 hours."
  noindex={true}
>
  <section class="thank-you-hero">
    <div class="container">
      <div class="success-icon">
        <i class="fas fa-check-circle"></i>
      </div>
      <h1>Thank You!</h1>
      <p class="hero-message">Your message has been sent successfully. We typically respond within <strong>2 hours</strong> during business hours.</p>
      <p class="confirmation-id">A confirmation will be sent to your email shortly.</p>
    </div>
  </section>

  <section class="section bg-light">
    <div class="container">
      <h2 class="section-title">What Happens Next?</h2>
      <div class="timeline">
        <div class="timeline-item">
          <div class="timeline-icon"><i class="fas fa-envelope-open-text"></i></div>
          <div class="timeline-content">
            <h3>We Review Your Request</h3>
            <p>Our team will carefully review your document requirements and prepare a detailed quote.</p>
          </div>
        </div>
        <div class="timeline-item">
          <div class="timeline-icon"><i class="fas fa-file-invoice-dollar"></i></div>
          <div class="timeline-content">
            <h3>Receive Your Quote</h3>
            <p>You'll receive a transparent quote with pricing, timeline, and delivery details via email or WhatsApp.</p>
          </div>
        </div>
        <div class="timeline-item">
          <div class="timeline-icon"><i class="fas fa-rocket"></i></div>
          <div class="timeline-content">
            <h3>We Start Working</h3>
            <p>Once approved, our MOJ-certified translators begin working on your documents immediately.</p>
          </div>
        </div>
      </div>
    </div>
  </section>

  <section class="section bg-white">
    <div class="container">
      <h2 class="section-title">While You Wait</h2>
      <p class="section-intro">Explore our resources to learn more about our services and pricing.</p>

      <div class="links-grid">
        <a href="/resources/pricing-guide/" class="link-card featured">
          <div class="link-icon"><i class="fas fa-calculator"></i></div>
          <h3>Pricing Guide</h3>
          <p>Understand translation costs, what affects pricing, and get budget estimates for your documents.</p>
          <span class="link-cta">View Pricing <i class="fas fa-arrow-right"></i></span>
        </a>

        <a href="https://wa.me/971508620217" class="link-card whatsapp" target="_blank" rel="noopener">
          <div class="link-icon"><i class="fab fa-whatsapp"></i></div>
          <h3>Chat with Us</h3>
          <p>Need faster assistance? Connect directly with our team on WhatsApp for instant support.</p>
          <span class="link-cta">Open WhatsApp <i class="fas fa-arrow-right"></i></span>
        </a>

        <a href="/resources/faq/" class="link-card">
          <div class="link-icon"><i class="fas fa-question-circle"></i></div>
          <h3>FAQ</h3>
          <p>Common questions about translation, certification, timelines, and the overall process.</p>
          <span class="link-cta">Read FAQ <i class="fas fa-arrow-right"></i></span>
        </a>

        <a href="/services/" class="link-card">
          <div class="link-icon"><i class="fas fa-concierge-bell"></i></div>
          <h3>Our Services</h3>
          <p>Explore our full range of legal translation and attestation services.</p>
          <span class="link-cta">View Services <i class="fas fa-arrow-right"></i></span>
        </a>

        <a href="/resources/document-checklist/" class="link-card">
          <div class="link-icon"><i class="fas fa-clipboard-check"></i></div>
          <h3>Document Checklist</h3>
          <p>Prepare your documents correctly before submitting for translation.</p>
          <span class="link-cta">View Checklist <i class="fas fa-arrow-right"></i></span>
        </a>

        <a href="/resources/attestation-guide/" class="link-card">
          <div class="link-icon"><i class="fas fa-stamp"></i></div>
          <h3>Attestation Guide</h3>
          <p>Learn about MOFA attestation, embassy legalization, and document authentication.</p>
          <span class="link-cta">Read Guide <i class="fas fa-arrow-right"></i></span>
        </a>
      </div>
    </div>
  </section>

  <section class="section bg-dark cta-section">
    <div class="container">
      <div class="cta-content">
        <h2>Need Urgent Assistance?</h2>
        <p>For time-sensitive requests, contact us directly via phone or WhatsApp.</p>
        <div class="cta-buttons">
          <a href="https://wa.me/971508620217" class="btn btn-whatsapp" target="_blank" rel="noopener">
            <i class="fab fa-whatsapp"></i> WhatsApp Us
          </a>
          <a href="tel:+971508620217" class="btn btn-outline">
            <i class="fas fa-phone"></i> +971 50 862 0217
          </a>
        </div>
      </div>
    </div>
  </section>

  <section class="section bg-light">
    <div class="container">
      <div class="return-home">
        <a href="/" class="btn btn-primary">
          <i class="fas fa-home"></i> Return to Homepage
        </a>
      </div>
    </div>
  </section>
</BaseLayout>

<style>
  /* Hero Section */
  .thank-you-hero {
    padding: 10rem 0 5rem;
    background: linear-gradient(135deg, var(--surface-navy) 0%, var(--surface-navy-dark) 100%);
    text-align: center;
    color: var(--text-on-dark);
  }

  .success-icon {
    font-size: 5rem;
    color: #4ade80;
    margin-bottom: 1.5rem;
    animation: scaleIn 0.5s ease-out;
  }

  @keyframes scaleIn {
    0% { transform: scale(0); opacity: 0; }
    50% { transform: scale(1.2); }
    100% { transform: scale(1); opacity: 1; }
  }

  .thank-you-hero h1 {
    font-size: clamp(2.5rem, 6vw, 4rem);
    font-weight: 800;
    margin-bottom: 1rem;
    text-transform: uppercase;
    letter-spacing: 0.05em;
  }

  .hero-message {
    font-size: 1.25rem;
    max-width: 600px;
    margin: 0 auto 1rem;
    color: var(--text-on-dark-muted);
    line-height: 1.7;
  }

  .hero-message strong {
    color: var(--accent-coral);
  }

  .confirmation-id {
    font-size: 0.95rem;
    color: var(--text-on-dark-subtle);
  }

  /* Section Styling */
  .section-title {
    font-size: 1.75rem;
    text-align: center;
    margin-bottom: 1rem;
    color: var(--text-heading);
  }

  .section-intro {
    text-align: center;
    color: var(--text-secondary);
    max-width: 600px;
    margin: 0 auto 2.5rem;
  }

  /* Timeline */
  .timeline {
    display: flex;
    flex-direction: column;
    gap: 2rem;
    max-width: 700px;
    margin: 2rem auto 0;
  }

  .timeline-item {
    display: flex;
    gap: 1.5rem;
    align-items: flex-start;
  }

  .timeline-icon {
    width: 60px;
    height: 60px;
    min-width: 60px;
    background: var(--accent-coral);
    border-radius: 50%;
    display: flex;
    align-items: center;
    justify-content: center;
    color: white;
    font-size: 1.5rem;
  }

  .timeline-content h3 {
    font-size: 1.15rem;
    margin-bottom: 0.5rem;
    color: var(--text-heading);
  }

  .timeline-content p {
    color: var(--text-secondary);
    line-height: 1.6;
    margin: 0;
  }

  /* Links Grid */
  .links-grid {
    display: grid;
    grid-template-columns: repeat(3, 1fr);
    gap: 1.5rem;
  }

  .link-card {
    background: var(--surface-base);
    border: 1px solid var(--border-light);
    border-radius: 1rem;
    padding: 2rem;
    text-decoration: none;
    color: inherit;
    transition: all 0.3s ease;
    display: flex;
    flex-direction: column;
  }

  .link-card:hover {
    transform: translateY(-4px);
    box-shadow: 0 12px 40px rgba(0, 0, 0, 0.1);
    border-color: var(--accent-coral);
  }

  .link-card.featured {
    background: linear-gradient(135deg, #fff7ed 0%, #ffedd5 100%);
    border-color: var(--accent-coral);
  }

  .link-card.whatsapp {
    background: linear-gradient(135deg, #dcfce7 0%, #bbf7d0 100%);
    border-color: #25D366;
  }

  .link-card.whatsapp:hover {
    border-color: #1da851;
  }

  .link-icon {
    width: 56px;
    height: 56px;
    border-radius: 14px;
    background: var(--surface-navy);
    color: white;
    display: flex;
    align-items: center;
    justify-content: center;
    font-size: 1.5rem;
    margin-bottom: 1.25rem;
  }

  .link-card.featured .link-icon {
    background: var(--accent-coral);
  }

  .link-card.whatsapp .link-icon {
    background: #25D366;
  }

  .link-card h3 {
    font-size: 1.15rem;
    margin-bottom: 0.5rem;
    color: var(--text-heading);
  }

  .link-card p {
    color: var(--text-secondary);
    font-size: 0.95rem;
    line-height: 1.6;
    margin: 0;
    flex-grow: 1;
  }

  .link-cta {
    display: inline-flex;
    align-items: center;
    gap: 0.5rem;
    margin-top: 1rem;
    color: var(--accent-coral);
    font-weight: 600;
    font-size: 0.9rem;
  }

  .link-card.whatsapp .link-cta {
    color: #25D366;
  }

  .link-cta i {
    transition: transform 0.2s ease;
  }

  .link-card:hover .link-cta i {
    transform: translateX(4px);
  }

  /* CTA Section */
  .cta-section {
    text-align: center;
    padding: 4rem 0;
  }

  .cta-content h2 {
    color: var(--text-on-dark);
    margin-bottom: 0.75rem;
  }

  .cta-content p {
    color: var(--text-on-dark-muted);
    margin-bottom: 2rem;
  }

  .cta-buttons {
    display: flex;
    gap: 1rem;
    justify-content: center;
    flex-wrap: wrap;
  }

  .btn {
    display: inline-flex;
    align-items: center;
    gap: 0.5rem;
    padding: 1rem 2rem;
    border-radius: 0.5rem;
    font-weight: 600;
    text-decoration: none;
    transition: all 0.3s ease;
  }

  .btn-whatsapp {
    background: #25D366;
    color: white;
  }

  .btn-whatsapp:hover {
    background: #1da851;
    transform: translateY(-2px);
  }

  .btn-outline {
    background: transparent;
    border: 2px solid rgba(255, 255, 255, 0.4);
    color: var(--text-on-dark);
  }

  .btn-outline:hover {
    background: rgba(255, 255, 255, 0.1);
    border-color: rgba(255, 255, 255, 0.6);
  }

  .btn-primary {
    background: var(--accent-coral);
    color: white;
  }

  .btn-primary:hover {
    background: var(--accent-coral-hover);
    transform: translateY(-2px);
  }

  /* Return Home */
  .return-home {
    text-align: center;
    padding: 2rem 0;
  }

  /* Responsive */
  @media (max-width: 992px) {
    .links-grid {
      grid-template-columns: repeat(2, 1fr);
    }
  }

  @media (max-width: 768px) {
    .thank-you-hero {
      padding: 8rem 0 4rem;
    }

    .success-icon {
      font-size: 4rem;
    }

    .links-grid {
      grid-template-columns: 1fr;
    }

    .timeline-item {
      flex-direction: column;
      text-align: center;
      align-items: center;
    }

    .cta-buttons {
      flex-direction: column;
      align-items: center;
    }

    .btn {
      width: 100%;
      max-width: 280px;
      justify-content: center;
    }
  }
</style>
