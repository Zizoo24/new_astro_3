---
/**
 * FooterArabic.astro - Arabic RTL Footer
 * Fully translated footer for Arabic pages
 */
import { siteContactAr, footerNavAr } from '../data/navigation-ar';
import LanguageSwitcher from './LanguageSwitcher.astro';

// Social links (same URLs, Arabic labels)
const socialLinksAr = [
  { icon: 'fab fa-whatsapp', url: siteContactAr.whatsapp, label: 'واتساب' },
  { icon: 'fab fa-facebook-f', url: 'https://facebook.com/onlinetranslationae', label: 'فيسبوك' },
  { icon: 'fab fa-instagram', url: 'https://instagram.com/onlinetranslationae', label: 'انستغرام' },
  { icon: 'fab fa-linkedin-in', url: 'https://linkedin.com/company/onlinetranslationae', label: 'لينكدإن' },
];
---

<footer class="footer footer-rtl bg-dark" data-theme="dark" dir="rtl">
  <div class="container">
    
    <!-- Main Footer Grid - RTL 4 columns -->
    <div class="footer-grid footer-grid-rtl">
      
      <!-- COLUMN 1: Brand -->
      <div class="footer-brand">
        <a href="/ar/" class="footer-logo">
          <div class="footer-logo-text">
            <span class="logo-name">Online<span class="logo-suffix">Translation</span><span class="logo-domain">.ae</span></span>
            <span class="logo-tagline">ترجمة قانونية معتمدة</span>
          </div>
          <img src="/assets/images/logo/brand/emblem-clean.png" alt="OnlineTranslation.ae" class="footer-logo-img" loading="lazy" width="48" height="48" />
        </a>
        <p>خدمات ترجمة معتمدة من شركاء حاصلين على اعتماد وزارة العدل. وثائق منسقة لمحاكم دبي ومركز دبي المالي العالمي وسوق أبوظبي العالمي والجهات الاتحادية وخدمات الهجرة.</p>
        
        <p class="footer-partnership">
          <i class="fas fa-certificate"></i>
          ترجمة معتمدة من وزارة العدل بواسطة <strong><a href="https://arkanlegaltranslation.ae/learn-abt-arkan/" target="_blank" rel="noopener" class="partner-link">أركان للترجمة القانونية</a></strong>، شريكنا ومترجمنا القانوني المرخص
        </p>
        
        <div class="footer-social">
          {socialLinksAr.map(social => (
            <a href={social.url} target="_blank" rel="noopener" aria-label={social.label}>
              <i class={social.icon}></i>
            </a>
          ))}
        </div>
      </div>
      
      <!-- COLUMN 2: Quick Links -->
      <div class="footer-links">
        <h4>روابط سريعة</h4>
        <ul>
          {footerNavAr.quickLinks.map(link => (
            <li><a href={link.href}>{link.label}</a></li>
          ))}
        </ul>
      </div>
      
      <!-- COLUMN 3: Our Services -->
      <div class="footer-links footer-services">
        <h4>خدماتنا</h4>
        <ul>
          {footerNavAr.popularServices.map(link => (
            <li><a href={link.href}>{link.label}</a></li>
          ))}
        </ul>
      </div>
      
      <!-- COLUMN 4: Contact Us -->
      <div class="footer-contact">
        <h4>تواصلوا معنا</h4>
        <ul>
          <li>
            <i class="fas fa-map-marker-alt"></i>
            <a href="https://maps.app.goo.gl/jz5HjwSF4aktSUrH7" target="_blank" rel="noopener" class="address-link">
              {siteContactAr.address}
              <span class="map-badge">عرض الخريطة</span>
            </a>
          </li>
          <li>
            <i class="fas fa-phone"></i>
            <a href={`tel:${siteContactAr.phone.replace(/\s/g, '')}`} dir="ltr">{siteContactAr.phone}</a>
          </li>
          <li>
            <i class="fas fa-envelope"></i>
            <a href={`mailto:${siteContactAr.email}`} dir="ltr">{siteContactAr.email}</a>
          </li>
          <li>
            <i class="fas fa-clock"></i>
            <span>يومياً: ٨:٠٠ صباحاً - ١٠:٠٠ مساءً</span>
          </li>
        </ul>
      </div>
      
    </div><!-- /.footer-grid -->
    
    <!-- Language Switcher Row -->
    <div class="footer-lang-row">
      <span class="footer-lang-label"><i class="fas fa-globe"></i> اللغة:</span>
      <LanguageSwitcher variant="pill" size="md" />
    </div>
    
    <!-- Footer Bottom -->
    <div class="footer-bottom">
      <p>© {new Date().getFullYear()} OnlineTranslation.ae. جميع الحقوق محفوظة. | <a href="/ar/privacy/">الخصوصية</a> | <a href="/ar/terms/">الشروط</a> | <a href="/sitemap-0.xml">خريطة الموقع</a></p>
    </div>
    
  </div>
</footer>

<style>
  /* ============================================
     ARABIC FOOTER - RTL STYLES
     ============================================ */
  .footer-rtl {
    direction: rtl;
    text-align: right;
    padding: 60px 0 30px;
    background: var(--surface-navy-dark, #072035);
    color: var(--text-on-dark, #f4f7fc);
  }
  
  .footer-grid-rtl {
    display: grid !important;
    grid-template-columns: 1.5fr 1fr 1fr 1fr !important;
    gap: 40px !important;
    align-items: start !important;
  }
  
  .footer-grid-rtl > * {
    min-width: 0;
  }
  
  /* RTL Brand Section */
  .footer-rtl .footer-brand {
    display: flex;
    flex-direction: column;
    align-items: flex-end;
  }
  
  .footer-rtl .footer-logo {
    margin-bottom: 1rem;
    display: flex;
    align-items: center;
    flex-direction: row-reverse;
    gap: 12px;
    text-decoration: none;
  }
  
  .footer-logo-img {
    height: 40px;
    width: auto;
  }
  
  .footer-logo-text {
    display: flex;
    flex-direction: column;
    text-align: right;
  }
  
  .footer-logo-text .logo-name {
    font-size: 1.1rem;
    font-weight: 700;
    color: #ffffff;
    letter-spacing: -0.01em;
    direction: ltr;
    text-align: right;
  }
  
  .footer-logo-text .logo-suffix {
    font-weight: 400;
  }
  
  .footer-logo-text .logo-domain {
    color: var(--accent-coral, #FF1654);
    font-weight: 700;
  }
  
  .footer-logo-text .logo-tagline {
    font-family: 'Noto Sans Arabic', var(--font-secondary), sans-serif;
    font-size: 0.75rem;
    color: var(--text-on-dark-muted, #c5d4e8);
    margin-top: 2px;
  }
  
  .footer-rtl .footer-brand > p {
    color: var(--text-on-dark-muted, #c5d4e8);
    font-size: 0.95rem;
    line-height: 1.8;
    margin-bottom: 1.25rem;
    font-family: 'Noto Sans Arabic', var(--font-secondary), sans-serif;
  }
  
  /* Arkan Partnership Pill - RTL */
  .footer-rtl .footer-partnership {
    display: inline-flex;
    align-items: center;
    flex-direction: row-reverse;
    gap: 8px;
    padding: 12px 20px;
    background: linear-gradient(135deg, rgba(212, 175, 55, 0.12) 0%, rgba(212, 175, 55, 0.08) 100%);
    border: 1px solid rgba(212, 175, 55, 0.25);
    border-radius: 50px;
    font-size: 0.85rem;
    margin-bottom: 1.5rem;
    font-family: 'Noto Sans Arabic', var(--font-secondary), sans-serif;
  }
  
  .footer-rtl .footer-partnership i {
    color: #f4d03f;
  }
  
  .footer-rtl .footer-partnership .partner-link {
    color: #f4d03f;
    font-weight: 600;
  }
  
  .footer-rtl .footer-partnership .partner-link:hover {
    color: #ffd700;
    text-decoration: underline;
  }
  
  /* Social Icons - RTL */
  .footer-rtl .footer-social {
    display: flex;
    flex-direction: row-reverse;
    gap: 12px;
  }
  
  .footer-rtl .footer-social a {
    display: flex;
    align-items: center;
    justify-content: center;
    width: 40px;
    height: 40px;
    background: rgba(255, 255, 255, 0.1);
    border-radius: 50%;
    color: var(--text-on-dark, #f4f7fc);
    transition: all 0.2s ease;
  }
  
  .footer-rtl .footer-social a:hover {
    background: var(--accent-coral, #FF1654);
    color: #fff;
    transform: translateY(-2px);
  }
  
  /* Column Headers - RTL */
  .footer-rtl .footer-links h4,
  .footer-rtl .footer-contact h4 {
    font-family: 'Noto Sans Arabic', var(--font-primary), sans-serif;
    font-size: 0.95rem;
    font-weight: 700;
    margin-bottom: 1.25rem;
    color: #ffffff;
    position: relative;
    padding-bottom: 0.75rem;
  }
  
  .footer-rtl .footer-links h4::after,
  .footer-rtl .footer-contact h4::after {
    content: '';
    position: absolute;
    bottom: 0;
    right: 0;
    left: auto;
    width: 30px;
    height: 2px;
    background: var(--accent-coral, #FF1654);
  }
  
  .footer-rtl .footer-links ul,
  .footer-rtl .footer-contact ul {
    list-style: none;
    padding: 0;
    margin: 0;
  }
  
  .footer-rtl .footer-links li {
    margin-bottom: 0.6rem;
  }
  
  .footer-rtl .footer-links a {
    color: var(--text-on-dark-muted, #c5d4e8);
    font-size: 0.95rem;
    font-family: 'Noto Sans Arabic', var(--font-secondary), sans-serif;
    transition: color 0.2s ease;
  }
  
  .footer-rtl .footer-links a:hover {
    color: var(--accent-coral, #FF1654);
  }
  
  /* Contact Items - Icon on LEFT, Text on RIGHT (even in RTL) */
  .footer-rtl .footer-contact li {
    display: flex;
    flex-direction: row;
    align-items: flex-start;
    gap: 10px;
    margin-bottom: 0.75rem;
    color: var(--text-on-dark-muted, #c5d4e8);
    font-size: 0.95rem;
    font-family: 'Noto Sans Arabic', var(--font-secondary), sans-serif;
    direction: ltr; /* Force LTR for icon-text layout */
  }

  .footer-rtl .footer-contact li i {
    color: var(--accent-coral, #FF1654);
    width: 16px;
    margin-top: 3px;
    flex-shrink: 0;
  }

  /* Text content stays RTL for Arabic text */
  .footer-rtl .footer-contact li a,
  .footer-rtl .footer-contact li span {
    direction: rtl;
    text-align: right;
  }

  /* Keep phone/email LTR for readability */
  .footer-rtl .footer-contact li a[dir="ltr"] {
    direction: ltr;
    text-align: left;
  }
  
  .footer-rtl .footer-contact a {
    color: var(--text-on-dark-muted, #c5d4e8);
    transition: color 0.2s ease;
  }
  
  .footer-rtl .footer-contact a:hover {
    color: var(--accent-coral, #FF1654);
  }

  /* Address with Map Link - RTL */
  .footer-rtl .address-link {
    display: flex;
    flex-direction: column;
    align-items: flex-end;
    gap: 6px;
  }

  .footer-rtl .map-badge {
    display: inline-flex;
    align-items: center;
    gap: 4px;
    font-size: 0.75rem;
    font-weight: 600;
    color: var(--accent-coral, #FF1654);
    opacity: 0.9;
  }

  .footer-rtl .map-badge::after {
    content: '\f35d';
    font-family: 'Font Awesome 5 Free';
    font-weight: 900;
    font-size: 0.65rem;
  }

  .footer-rtl .address-link:hover .map-badge {
    opacity: 1;
    text-decoration: underline;
  }
  
  /* Language Switcher Row */
  .footer-lang-row {
    display: flex;
    align-items: center;
    justify-content: center;
    gap: 16px;
    margin-top: 30px;
    padding-top: 24px;
    border-top: 1px solid rgba(255, 255, 255, 0.1);
  }
  
  .footer-lang-label {
    display: flex;
    align-items: center;
    gap: 8px;
    color: var(--text-on-dark-muted, #c5d4e8);
    font-size: 0.9rem;
    font-family: 'Noto Sans Arabic', var(--font-secondary), sans-serif;
  }
  
  .footer-lang-label i {
    color: var(--accent-coral, #FF1654);
  }
  
  /* Footer Bottom - RTL */
  .footer-rtl .footer-bottom {
    margin-top: 24px;
    padding-top: 24px;
    border-top: 1px solid rgba(255, 255, 255, 0.1);
    text-align: center;
  }
  
  .footer-rtl .footer-bottom p {
    font-size: 0.875rem;
    color: var(--text-on-dark-subtle, #9fb2c9);
    margin: 0;
    font-family: 'Noto Sans Arabic', var(--font-secondary), sans-serif;
  }
  
  .footer-rtl .footer-bottom a {
    color: var(--text-on-dark-muted, #c5d4e8);
  }
  
  .footer-rtl .footer-bottom a:hover {
    color: var(--accent-coral, #FF1654);
  }
  
  /* ============================================
     MOBILE - RTL STACK
     ============================================ */
  @media (max-width: 991px) {
    .footer-rtl {
      padding: 40px 0 100px;
    }

    .footer-rtl .container {
      padding-left: 20px;
      padding-right: 20px;
    }

    .footer-grid-rtl {
      display: block !important;
      text-align: center;
    }

    .footer-grid-rtl > * {
      display: block !important;
      width: 100% !important;
      margin-bottom: 32px;
      text-align: center;
    }

    .footer-grid-rtl > *:last-child {
      margin-bottom: 0;
    }

    .footer-rtl .footer-brand {
      display: flex !important;
      flex-direction: column !important;
      align-items: center !important;
    }

    .footer-rtl .footer-brand > p {
      text-align: center;
      max-width: 320px;
      margin-left: auto;
      margin-right: auto;
    }

    .footer-rtl .footer-logo {
      flex-direction: column;
      gap: 8px;
      justify-content: center;
      align-items: center;
    }

    .footer-rtl .footer-logo-text {
      text-align: center;
    }

    .footer-rtl .footer-partnership {
      margin-left: auto;
      margin-right: auto;
      text-align: center;
      flex-wrap: wrap;
      justify-content: center;
      flex-direction: row;
    }

    .footer-rtl .footer-links,
    .footer-rtl .footer-services,
    .footer-rtl .footer-contact {
      display: flex !important;
      flex-direction: column !important;
      align-items: center !important;
    }

    .footer-rtl .footer-links h4,
    .footer-rtl .footer-services h4,
    .footer-rtl .footer-contact h4 {
      text-align: center;
      width: auto;
      display: inline-block;
    }

    .footer-rtl .footer-links h4::after,
    .footer-rtl .footer-services h4::after,
    .footer-rtl .footer-contact h4::after {
      left: 50% !important;
      right: auto !important;
      transform: translateX(-50%) !important;
    }

    .footer-rtl .footer-links ul,
    .footer-rtl .footer-services ul {
      display: flex;
      flex-direction: column;
      align-items: center;
      gap: 10px;
    }

    .footer-rtl .footer-contact ul {
      display: flex;
      flex-direction: column;
      align-items: center;
      gap: 12px;
    }

    .footer-rtl .footer-contact li {
      flex-direction: row;
      justify-content: center;
    }

    .footer-rtl .footer-social {
      justify-content: center;
      flex-direction: row;
      width: 100%;
    }
    
    .footer-lang-row {
      flex-direction: column;
      gap: 12px;
    }

    .footer-rtl .footer-bottom {
      margin-top: 32px;
      padding: 20px 16px;
    }
  }

  @media (max-width: 480px) {
    .footer-rtl .container {
      padding-left: 16px;
      padding-right: 16px;
    }

    .footer-grid-rtl > * {
      margin-bottom: 28px;
    }

    .footer-rtl .footer-partnership {
      padding: 10px 16px;
      font-size: 0.8rem;
    }
  }
</style>
